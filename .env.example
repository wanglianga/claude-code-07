# 复制为 .env 后按需修改；compose 会自动读取
# 项目名（并行任务由平台注入，勿随意修改）
COMPOSE_PROJECT_NAME=task01
# 前端发布到宿主的端口（唯一对外端口）
CC_PUBLISH_PORT=3000
# 数据库（仅 compose 内部网络访问，不对外发布）
DB_USER=postgres
DB_PASS=postgres
DB_NAME=meal_platform
# JWT 密钥，生产环境务必修改
JWT_SECRET=please-change-me-in-production
